# 切换生产环境地址使用
prod:
  host: 192.168.8.116
spring:
  application:
    name: product
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev
      label: master